# 균열 모니터 실행 및 기록

기존 카메라·YOLO 실행 환경에 Flask가 필요합니다 (`python -m pip install flask`).

```powershell
python crack_monitor.py
```

브라우저에서 http://127.0.0.1:5000 에 접속합니다. HTML 파일을 직접 열지 않습니다.

화면 파일은 역할별로 나눴습니다.

- `dashboard.html`: 화면 구조
- `static/css/dashboard.css`: 화면 스타일 및 인쇄·반응형 스타일
- `static/js/dashboard.js`: API 조회, 추정 설정창, 표·보고서 갱신

Flask가 `/static/` 경로로 CSS와 JavaScript를 제공합니다. JavaScript는 `defer`로 HTML 해석 후 실행합니다.

## 첫 접속 추정 설정

현재는 단일 깊이를 입력하지 않고 **내부 깊이 1·3·5·10cm별 비교표**를 표시합니다. 설정창의 1m 미리보기, 현재 탐지 결과, 손상별 상세와 보고서 합계에 적용됩니다. 각 행은 대안 시나리오이므로 서로 합산하지 않습니다. 표면처리 비용은 내부 깊이와 무관합니다. 과거 단일 깊이 기록은 원본을 보존하되 비교표 합계에는 미산출로 표시합니다. 기존 설정의 `depth_mm`은 호환용으로 유지하지만 새 비교 계산에는 사용하지 않습니다.

처음 접속하면 ‘보수비 추정 기준을 입력해 주세요’ 창이 열립니다. 예시값을 확인·수정한 뒤 ‘이 값으로 저장하고 시작’을 누르세요. 저장 전에는 비용 추정을 보류합니다. 설정창을 닫아도 영상 관측은 가능합니다.

- 예시값은 공식 노임·시장가격·측정값이 아닌 가정입니다. 특별인부 220,000원/일, 보통인부 170,000원/일, 미장공 270,000원/일, 재료 25,000원/kg을 입력용 초기값으로 둡니다.
- 주입공법의 기본 가정은 폭 0.3mm, 밀도 1.1kg/L, 사용량 보정 1.2배입니다. 폭은 가정값 또는 카메라 관측 대표 폭 중 선택할 수 있습니다. 내부 깊이 1·3·5·10cm는 모두 가정값이며 측정 결과가 아닙니다.
- 표면처리공법은 깊이 대신 kg/m 사용량을 입력합니다. 초기값은 0.1kg/m입니다. 잡재료율은 0%로 시작합니다.
- 저장한 값은 `estimate_setup.json`에 보관하여 새로고침·서버 재시작 후에도 사용합니다. 브라우저별 설정이 아니라 이 서버를 사용하는 모든 화면에 공유되는 설정입니다.
- 사이드바 ‘보수비 추정 설정’에서 다시 수정할 수 있습니다. ‘예시값 다시 채우기’는 화면만 바꾸며 저장 버튼을 눌러야 적용됩니다.
- 이후 관측에 적용하며 과거 기록 전체를 재계산하지 않습니다. 연속 추적 중인 손상은 다음 이력 갱신에서 새 설정으로 계산됩니다.

주입재 사용량(kg/m)은 `폭(mm) × 가정 깊이(mm) ÷ 1000 × 밀도(kg/L) × 보정 배수`입니다. 내부가 균일한 직사각형 틈이라는 단순 가정이며 표준품셈이 정한 재료량 공식은 아닙니다. 화면의 1m 미리보기에는 가정 길이를, 실제 관측 비용에는 영상으로 산출한 길이를 사용합니다.

## 관측 이력

### 틈폭과 변화 기록

영상의 자홍색 `WMAX`는 중심선 거리변환에서 폭이 가장 큰 지점입니다. 실제 최대 근사 폭, 대표 폭(중앙값), P95 폭을 구분합니다. 기존 `max_width_mm`은 호환을 위해 P95 값을 유지하며 `raw_max_width_mm`이 실제 최댓값입니다. 중심선에서 마스크 경계까지 거리의 두 배를 사용하는 근사이므로 곡률·분기점·분할 오차에 영향을 받습니다. 가로/세로 바운딩 박스 길이를 틈폭으로 사용하지 않습니다.

보고서 손상 상세의 ‘틈폭 관측’에 최초 유효 관측 대비 P95 차이와 직전 대비 차이를 표시합니다. 최근 20회는 화면에서 펼쳐 볼 수 있고 전체 샘플은 SQLite의 `width_samples` 테이블에 보관합니다. 같은 손상의 샘플은 손상 수나 비용을 중복 집계하지 않습니다.

보정 방식·마커 ID가 바뀌거나 축척 차이가 10%를 초과하면 비교를 보류합니다. 보정값 유지 중이거나 P95가 2픽셀 상당 이하인 경우도 제외합니다. 초기/현재 각 2픽셀 상당을 합한 값을 비교 한계로 사용합니다. 이 수치는 프로젝트 품질 필터일 뿐 계측 불확도나 공인 성장 판정 기준이 아닙니다. 영상 위치 추적은 물리적으로 동일한 측정점의 장기 추적을 보장하지 않으며, 변화는 ‘관측값 차이’로만 해석합니다.

폭 기준 a~e는 기존 프로젝트 예비 분류를 유지합니다. 해당 숫자를 공식 구조 안전등급으로 검증한 것은 아닙니다. [FHWA 교량 콘크리트 균열 기록 절차](https://www.fhwa.dot.gov/publications/research/infrastructure/structures/ltbp/16007/049.cfm)는 균열 폭·위치 등 기록의 참고 자료이며, 이 프로젝트의 P95·예비구간·변화 필터 수치의 출처는 아닙니다.

- 같은 점검 정보에서 연속 프레임의 검출 영역이 겹치면 같은 손상으로 추적합니다. 최대 5초 미검출까지 유지하며, 위치가 겹치는 여러 균열은 일대일로 연결합니다.
- 같은 손상의 최신 측정값으로 보고서를 갱신합니다. 반복 관측 횟수만큼 손상 수와 보수비를 더하지 않습니다. 기록 주기는 기본 3초, 위험 탐지 시 1초입니다.
- `observations.sqlite3`에 이력을 저장합니다. 재시작해도 기존 손상 번호, 측정값, 캡처 연결이 유지되며 500건 초과 이력도 삭제하지 않습니다.
- 추적은 영상 위치 기반입니다. 카메라 이동, 장시간 미검출, 재연결·재시작 후 재관측은 별도 손상으로 기록될 수 있습니다. 동일 화면 위치로 다른 균열이 들어오면 잘못 연결될 수도 있습니다. 점검 위치가 바뀌면 부재·위치 정보를 변경하세요.
- 보고서의 ‘이력 삭제’는 DB 기록을 삭제합니다. PNG·JSON 캡처 파일은 그대로 남습니다. 관측을 계속하면 이후 기록 주기에 다시 등록됩니다.
- 기존 실행의 메모리에만 있던 이력이나 과거 캡처 JSON을 자동으로 가져오지는 않습니다.

## 측정 한계

폭이 `2 × mm/픽셀` 이하이면 등급은 ‘판정 보류’로 표시하며 위험 등급 자동 캡처 대상에서 제외합니다. 이 기준은 코드의 해상도 필터이며 실제 측정 정확도를 보증하지 않습니다.

## 검증

기존 근거 기반 계산기의 자료는 [보수비 산출 근거](REPAIR_COST_BASIS.md)를 참고하세요. 현재 웹 모니터는 위의 추정 설정을 사용하며 설정 파일을 직접 편집할 필요가 없습니다.

```powershell
python -m unittest test_observation_store -v
```

카메라 없이 중복 집계, 추적 분리, DB 재개방, 500건 초과 보존, 캡처 연결, 삭제, 측정 한계 경계를 확인합니다.
# Teensy IMU 추가 기능

Teensy 4.0 + GY-9250의 방향 화살표·8방향·가속도·실험용 속도/변위를 대시보드에서 표시합니다. 배선, 가상환경, 펌웨어 업로드, 자력계 보정, 데모와 ROS2 확장은 [IMU 사용 설명서](imu/README.md)에 있습니다. `imu/bridge.py`를 카메라 서버와 함께 실행해야 합니다. IMU만 적분한 속도와 변위는 정밀 오도메트리가 아니며 제어·안전 판단용으로 사용하지 마세요.
